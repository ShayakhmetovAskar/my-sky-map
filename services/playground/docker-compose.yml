services:
  playground:
    image: nginx:alpine
    container_name: playground_dev
    volumes:
      - ../../frontend/playground:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "5500:80"
